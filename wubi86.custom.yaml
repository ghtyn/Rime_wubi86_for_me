patch:
  # --- 基础翻译器设置 ---
  translator/dictionary: wubi86_user
  translator/enable_sentence: false
  # translator/enable_user_dict: false
  # translator/enable_completion: false

  # --- 拼写与自动上屏 ---
  speller/max_code_length: 4
  speller/auto_select: true

  # --- 简繁转换 ---
  simplifier:
    opencc_config: s2t.json
    option_name: zh_trad

  # --- 状态开关 ---
  switches:
    - name: ascii_mode
      reset: 1
      states: [ 中, 西 ]
    - name: full_shape
      states: [ 半, 全 ]
    - name: extended_charset
      states: [ 常, 增 ]
    - name: ascii_punct
      states: [ 。, ． ]
    - name: zh_trad
      reset: 0
      states: [ 汉, 漢 ]
    - name: emoji
      states: [ 🈚️, 🈶 ]
      reset: 1

  # --- Lua 组件挂载 ---
  engine/processors/@before 0: "lua_processor@pin_word_processor"

  engine/filters:
    - "lua_filter@pin_word_filter"
    - simplifier            # 简繁转换
    - simplifier@emoji      # Emoji转换
    - uniquifier            # 过滤重复项

  engine/translators/+:
    - "lua_translator@date_translator"

  # --- Emoji 设置 ---
  emoji:
    option_name: emoji
    opencc_config: emoji.json
    inherit_comment: false

  # --- 快捷键变量定义与绑定 ---
  key_binder/+:
    import_preset: default
    pin_cand: "Control+t"     # 定义：置顶/取消置顶 变量
    unpin_cand: "Control+t"   # 定义：取消置顶 变量
    del_cand: "Control+d"     # 定义：屏蔽词条 变量
    bindings:
      - {accept: semicolon, send: 2, when: has_menu}   # 分号选二重
      - {accept: apostrophe, send: 3, when: has_menu}  # 引号选三重
      - {accept: bracketleft, send: Page_Up, when: paging}
      - {accept: bracketright, send: Page_Down, when: has_menu}